'use strict';

describe('Service: qxComponentObject40', function () {

  // load the service's module
  beforeEach(module('spafelizApp'));

  // instantiate service
  var qxComponentObject40;
  beforeEach(inject(function (_qxComponentObject40_) {
    qxComponentObject40 = _qxComponentObject40_;
  }));

  it('should do something', function () {
    expect(!!qxComponentObject40).toBe(true);
  });

});
