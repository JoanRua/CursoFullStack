'use strict';

describe('Service: qxComponentObject33', function () {

  // load the service's module
  beforeEach(module('spafelizApp'));

  // instantiate service
  var qxComponentObject33;
  beforeEach(inject(function (_qxComponentObject33_) {
    qxComponentObject33 = _qxComponentObject33_;
  }));

  it('should do something', function () {
    expect(!!qxComponentObject33).toBe(true);
  });

});
