'use strict';

describe('Service: qxComponentObject26', function () {

  // load the service's module
  beforeEach(module('spafelizApp'));

  // instantiate service
  var qxComponentObject26;
  beforeEach(inject(function (_qxComponentObject26_) {
    qxComponentObject26 = _qxComponentObject26_;
  }));

  it('should do something', function () {
    expect(!!qxComponentObject26).toBe(true);
  });

});
