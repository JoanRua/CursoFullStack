'use strict';

describe('Service: qxComponentObject28', function () {

  // load the service's module
  beforeEach(module('spafelizApp'));

  // instantiate service
  var qxComponentObject28;
  beforeEach(inject(function (_qxComponentObject28_) {
    qxComponentObject28 = _qxComponentObject28_;
  }));

  it('should do something', function () {
    expect(!!qxComponentObject28).toBe(true);
  });

});
