'use strict';

describe('Service: qxComponentObject38', function () {

  // load the service's module
  beforeEach(module('spafelizApp'));

  // instantiate service
  var qxComponentObject38;
  beforeEach(inject(function (_qxComponentObject38_) {
    qxComponentObject38 = _qxComponentObject38_;
  }));

  it('should do something', function () {
    expect(!!qxComponentObject38).toBe(true);
  });

});
