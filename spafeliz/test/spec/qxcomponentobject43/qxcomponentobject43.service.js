'use strict';

describe('Service: qxComponentObject43', function () {

  // load the service's module
  beforeEach(module('spafelizApp'));

  // instantiate service
  var qxComponentObject43;
  beforeEach(inject(function (_qxComponentObject43_) {
    qxComponentObject43 = _qxComponentObject43_;
  }));

  it('should do something', function () {
    expect(!!qxComponentObject43).toBe(true);
  });

});
