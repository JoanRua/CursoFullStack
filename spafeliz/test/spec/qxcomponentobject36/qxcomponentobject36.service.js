'use strict';

describe('Service: qxComponentObject36', function () {

  // load the service's module
  beforeEach(module('spafelizApp'));

  // instantiate service
  var qxComponentObject36;
  beforeEach(inject(function (_qxComponentObject36_) {
    qxComponentObject36 = _qxComponentObject36_;
  }));

  it('should do something', function () {
    expect(!!qxComponentObject36).toBe(true);
  });

});
